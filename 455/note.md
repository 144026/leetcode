use `qsort(void* base, size_t Num, size_t Size, int* compare(void* , void* ) )` function in `stdlib.h`.

traverse the cookie-array, try to assign current cookie to current kid. If can't, try next cookie.
